<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="action_export_stock_adjustment" model="ir.actions.server">
        <field name="name">Export Stock adjustment</field>
        <field name="model_id" ref="inventory_stock_adjustments.model_stock_inventory"/>
        <field name="binding_model_id" ref="inventory_stock_adjustments.model_stock_inventory"/>
        <field name="state">code</field>
        <field name="code">
            action = records.action_export_stock_adjustment()
        </field>
    </record>

</odoo>
